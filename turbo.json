{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": {
            "outputs": ["dist/**"],
            "dependsOn": ["^build"]
        },
        "//#test": {
            "inputs": ["packages/**/src/**", "apps/**/src/**"]
        },
        "//#lint": {
            "inputs": ["packages/**/src/**", "apps/**/src/**"]
        }
    }
}
