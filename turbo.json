{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build": {
            "outputs": ["dist/**"],
            "dependsOn": ["^build"]
        },
        "//#test": {
            "inputs": ["packages/**/src/**", "apps/**/src/**"]
        },
        "//#lint": {
            "inputs": ["packages/**/src/**", "apps/**/src/**"]
        }
    }
}
